5780b7de954e5481f5de598de1eebedc
// const knex = jest.fn();
const mysql = jest.fn();
let knex = jest.mock('knex');
const fp = require('fastify-plugin');
const knexPlugin = fp(async (a) => {
    console.log(a);
    return jest.fn()
});
jest.mock('./src/plugins/knex-plugin', (() => {
  return knexPlugin;
}));

knex = {
    raw: jest.fn().mockReturnThis(),
    destroy: jest.fn().mockReturnThis(),
}