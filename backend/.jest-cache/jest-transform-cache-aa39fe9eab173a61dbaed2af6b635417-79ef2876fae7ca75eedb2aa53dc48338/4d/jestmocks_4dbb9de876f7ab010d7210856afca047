54f597a433fa547bfa86ad9c99c5cbce
// const knex = jest.fn();
const mysql = jest.fn();
const Knex = require('knex');
const fp = require('fastify-plugin');

Knex.knex = jest.mock();

const knexPlugin = async () => ({
    raw: jest.fn(),
    destroy: jest.fn(),
})

jest.mock('./src/plugins/knex-plugin', (() => {
  return knexPlugin;
}));

// knex = {
//     raw: jest.fn().mockReturnThis(),
//     destroy: jest.fn().mockReturnThis(),
// }
