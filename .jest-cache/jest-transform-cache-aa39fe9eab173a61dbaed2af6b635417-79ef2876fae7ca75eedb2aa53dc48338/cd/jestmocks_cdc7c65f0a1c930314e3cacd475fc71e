aa12b64b1a814d855b8b45f4fc1429ea
// const knex = jest.fn();
const mysql = jest.fn();
let knex = jest.mock('knex');
const fp = require('fastify-plugin');

const knexPlugin = async () => ({
    raw: jest.fn(),
    destroy: jest.fn(),
})

jest.mock('./src/plugins/knex-plugin', (() => {
  return knexPlugin;
}));

// knex = {
//     raw: jest.fn().mockReturnThis(),
//     destroy: jest.fn().mockReturnThis(),
// }
